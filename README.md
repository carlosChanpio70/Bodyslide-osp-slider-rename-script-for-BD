# Bodyslide-osp-slider-rename-script

The script runs through all osp files, checks the body mod so it doesn't rename that part of the osp, and changes the slides with unique names

Just replace the 2 paths accordingly and run the script.
